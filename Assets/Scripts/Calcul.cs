using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Calcul
{
	public float Percentage(float _num, float _maxNum)
    {
        return _num / (100 / _maxNum);
    }
}
